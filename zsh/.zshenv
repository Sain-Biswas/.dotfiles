#! /usr/bin/env zsh

# add scripts to path
export PATH="$HOME/.dotfiles/scripts:$PATH"

# default programs
export EDITOR="nvim"
export VISUAL="nvim"

# color man pages
export MANPAGER="less -R --use-color -Dd+r -Du+b"
